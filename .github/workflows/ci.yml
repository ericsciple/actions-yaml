on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: npm ci
      - run: npm run format-check
      - run: npm run build
      - run: npm run lint
      - run: npm run test